<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
    <property resource="application.properties"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%date{ISO8601} [%thread] %-5level %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="TRANSACTION_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%date{ISO8601} [%thread] [%X{transactionKey}] %-5level %logger - %msg%n</pattern>
        </encoder>
    </appender>
    <property name="LOG_DIR" value="${transaction-id.tracking.log.dir}"/>

    <appender name="TRANSACTION_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/transaction.log/file</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/transaction.%d{yyyy-MM-dd}.%i.log.zip</fileNamePattern>
            <maxFileSize>5MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%date{ISO8601} [%thread] [%X{transactionKey}] %-5level %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>

    <logger name="ecommerce.personal.com" level="DEBUG" additivity="false">
        <appender-ref ref="TRANSACTION_CONSOLE"/>
        <appender-ref ref="TRANSACTION_FILE"/>
    </logger>
</configuration>